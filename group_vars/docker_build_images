# the provided role_volumes don't have names or persistent storage options, so they only affect Dockerfile.
# but it is perfectly acceptable to have a common/host volume that has the same path as one of these defaults.
role_volumes:
    - path: "{{ splunk_home }}/var"
      mount_type: image_volume
